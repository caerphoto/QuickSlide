/*globals chrome */
/* Intermediate file that fetches config state from the background page and
 * translates it into a QuickSlideConfig object. */
var QuickSlideConfig;

QuickSlideConfig = QuickSlideConfig || {};
QuickSlideConfig.chrome_extension = true;
